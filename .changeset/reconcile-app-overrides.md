---
'@viamrobotics/tailwind-config': minor
---

Add `danger`/`success`/`info` `bright` color tokens (with derived dark variants), a `popup-enter` animation token, a `button` pointer-cursor base style, and arbitrary-value support to the slider/scrollbar sizing utilities. Also updates `warning-bright` from `#ddab3f` to `#efa300`.
