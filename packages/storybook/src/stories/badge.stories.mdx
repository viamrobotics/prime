import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Badge } from '@viamrobotics/prime-core';
import { cx } from '../lib/cx-control';

<Meta
  title='Elements/Badge'
  argTypes={{
    variant: {
      description: 'The badge color',
      control: { type: 'select' },
      options: ['gray', 'green', 'orange', 'red', 'blue'],
      table: { defaultValue: { summary: 'gray' } },
    },
    label: {
      description: 'The badge status',
      table: { defaultValue: { summary: '' } },
    },
    cx,
  }}
/>

# Badge

To display status of something

<Canvas>
  <Story
    name='Neutral'
    args={{
      variant: 'gray',
      label: 'Inactive',
    }}
  >
    {(props) => ({
      Component: Badge,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Success'
    args={{
      variant: 'green',
      label: 'Active',
    }}
  >
    {(props) => ({
      Component: Badge,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Warning'
    args={{
      variant: 'orange',
      label: 'Danger',
    }}
  >
    {(props) => ({
      Component: Badge,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Danger'
    args={{
      variant: 'red',
      label: 'Unhealthy',
    }}
  >
    {(props) => ({
      Component: Badge,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Info'
    args={{
      variant: 'blue',
      label: 'Info',
    }}
  >
    {(props) => ({
      Component: Badge,
      props,
    })}
  </Story>
</Canvas>
