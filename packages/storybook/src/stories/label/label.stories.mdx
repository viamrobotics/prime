import { Canvas, Meta, Story } from '@storybook/addon-docs';
import WithInput from './with-input.svelte';
import { cx } from '../../lib/cx-control';

<Meta
  title='Elements/Label'
  argTypes={{
    position: {
      description: 'The desired position for the label relative to its input',
      control: 'select',
      options: ['top', 'left'],
      table: { defaultValue: { summary: 'top' } },
    },
    disabled: {
      description: 'Whether to render the label as disabled or not',
      control: { type: 'boolean' },
      table: { defaultValue: { summary: false } },
    },
    required: {
      description: 'Whether to render the label as required or not',
      control: { type: 'boolean' },
      table: { defaultValue: { summary: false } },
    },
    cx,
  }}
/>

# Label

For labelling inputs

<Canvas>
  <Story name='With Input'>
    {(props) => ({
      Component: WithInput,
      props,
    })}
  </Story>
</Canvas>
