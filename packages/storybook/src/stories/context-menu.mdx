import { Canvas, Meta, Story } from '@storybook/addon-docs';
import * as ContextMenuStories from './context-menu.stories.svelte';

<Meta title='Elements/Context menu' />

# Context menu

To display options for some context. Usually made visible on click (especially right click) and in conjunction with floating-ui.

```ts
import {
  ContextMenu,
  ContextMenuItem,
  ContextMenuSeparator,
} from '@viamrobotics/prime-core';
```

<Canvas>
  <div class='flex items-start gap-4'>
    <Story of={ContextMenuStories.Basic} />
    <Story of={ContextMenuStories.Floating} />
  </div>
</Canvas>
