import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Radio } from '@viamrobotics/prime-core';

<Meta
  title='Elements/Radio'
  parameters={{
    actions: {
      handles: ['input'],
    },
  }}
  argTypes={{
    options: {
      description: 'The list of options',
      table: { defaultValue: { summary: '' } },
    },
    selected: {
      description: 'The selected option',
      table: { defaultValue: { summary: '' } },
    },
    label: {
      description: 'An optional label',
      table: { defaultValue: { summary: '' } },
    },
    labelposition: {
      description: 'The position for the optional label',
      table: { defaultValue: { summary: 'top' } },
      control: { type: 'radio' },
      options: ['top', 'left'],
      type: 'text',
    },
    readonly: {
      description: 'If the radio is readonly',
      table: { defaultValue: { summary: 'false' } },
      control: { type: 'boolean' },
    },
    tooltip: {
      description: 'An optional tooltip',
      table: { defaultValue: { summary: '' } },
    },
    state: {
      description:
        'The state for the optional tooltip (determines the icon type)',
      table: { defaultValue: { summary: 'info' } },
      control: { type: 'radio' },
      options: ['info', 'warn', 'error'],
      type: 'text',
    },
    width: {
      description: 'Sets the width of the radio',
      options: ['default', 'full'],
      table: { defaultValue: { summary: 'default' } },
    },
    'on:input': {
      description: 'Event fired when an option is selected',
    },
  }}
/>

# Radio

For user triggered actions

<Canvas>
  <Story
    name='Radio'
    args={{
      options: ['Opt 1', 'Opt 2', 'Opt 3'],
      selected: 'Opt 1',
    }}
  >
    {(props) => ({
      Component: Radio,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='With label'
    args={{
      label: 'These are your options',
      labelposition: 'top',
      options: ['Opt 1', 'Opt 2'],
      selected: 'Opt 1',
    }}
  >
    {(props) => ({
      Component: Radio,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Tooltip and state'
    args={{
      label: 'Your options',
      labelposition: 'top',
      options: ['Option 1', 'Option 2', 'Option 3'],
      selected: 'Option 1',
      tooltip: 'Warning: these options may not be your only options.',
      state: 'warn',
    }}
  >
    {(props) => ({
      Component: Radio,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Readonly'
    args={{
      options: ['Opt 1', 'Opt 2', 'Opt 3'],
      selected: 'Opt 1',
      readonly: true,
    }}
  >
    {(props) => ({
      Component: Radio,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Full width radio'
    args={{
      options: ['Opt 1', 'Opt 2', 'Opt 3'],
      selected: 'Opt 1',
      width: 'full',
    }}
  >
    {(props) => ({
      Component: Radio,
      props,
    })}
  </Story>
</Canvas>
