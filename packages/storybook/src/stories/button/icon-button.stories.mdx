import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { IconButton } from '@viamrobotics/prime-core';
import { cx } from '../../lib/cx-control';

<Meta
  title='Elements/Button/IconButton'
  parameters={{
    actions: {
      handles: ['click'],
    },
  }}
  argTypes={{
    icon: {
      description: 'The icon to render',
      control: 'text',
      table: { defaultValue: { summary: 'play-circle-outline' } },
    },
    label: {
      description: 'The aria-label to apply for the button',
      control: 'text',
      table: { defaultValue: { summary: 'Run' } },
    },
    variant: {
      description: 'The communicated action type of the button',
      control: 'select',
      options: ['primary', 'danger'],
      table: { defaultValue: { summary: 'primary' } },
    },
    type: {
      description: 'The type attribute for the button',
      control: 'select',
      options: ['button', 'submit', 'reset'],
      table: { defaultValue: { summary: 'button' } },
    },
    title: {
      description: 'The title to apply for the button',
      table: { defaultValue: { summary: '' } },
    },
    cx,
  }}
/>

# Button

For user triggered actions

<Canvas>
  <Story
    name='Primary'
    args={{
      icon: 'play-circle-outline',
      label: 'Run',
    }}
  >
    {(props) => ({
      Component: IconButton,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Danger'
    args={{
      label: 'Stop',
      icon: 'stop-circle-outline',
      variant: 'danger',
    }}
  >
    {(props) => ({
      Component: IconButton,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Disabled'
    args={{
      icon: 'play-circle-outline',
      disabled: 'true',
      label: 'Run',
    }}
  >
    {(props) => ({
      Component: IconButton,
      props,
    })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='With title'
    args={{
      icon: 'play-circle-outline',
      label: 'Run',
      title: 'Run this command',
    }}
  >
    {(props) => ({
      Component: IconButton,
      props,
    })}
  </Story>
</Canvas>
