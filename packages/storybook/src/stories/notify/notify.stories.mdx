import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Button, Notify } from '@viamrobotics/prime-core';
import NotifySlot from './notify.spec.svelte';


<Meta
  title='Elements/Notify'
  parameters={{
    actions: {
      handles: ['close'],
    },
  }}
  argTypes={{
    variant: {
      control: { type: 'select' },
      options: ['danger', 'warning', 'success', 'info'],
      description: 'Notification type',
      table: { defaultValue: { summary: 'info' } },
    },
    title: {
      description: 'Title',
      table: { defaultValue: { summary: '' } },
    },
    message: {
      description: 'Message',
      table: { defaultValue: { summary: '' } },
    },
  }}
/>

# Notify

Notify message of type info, success, warning, or error

<Canvas>
  <Story
    name='info'
    args={{
      variant: 'info',
      title: 'This is the title',
      message: 'This is the message.',
    }}
  >
  {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='success'
    args={{
      variant: 'success',
      title: 'This is the title',
      message: 'This is the message.',
    }}
  >
  {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='warning'
    args={{
      variant: 'warning',
      title: 'This is the title',
      message: 'This is the message.',
    }}
  >
    {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='danger'
    args={{
      variant: 'danger',
      title: 'This is the title',
      message: 'This is the message.',
    }}
  >
    {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='no message info'
    args={{
      variant: 'info',
      title: 'This is the title',
    }}
  >
    {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='no message success'
    args={{
      variant: 'success',
      title: 'This is the title',
    }}
  >
    {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='no message warning'
    args={{
      variant: 'warning',
      title: 'This is the title',
    }}
  >
    {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='no message danger'
    args={{
      variant: 'danger',
      title: 'This is the title',
    }}
  >
  {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Exitable'
    args={{
      exitable: true,
      variant: 'danger',
      title: 'The roof is on fire',
      message: "Water is required to prevent further burning.",
      onclose:"alert('close')"

    }}
  >
  {(props) => ({
      Component: Notify,
      props,
  })}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Slot'
    args={{
      variant: 'info',
      title: 'You have an incoming life update',
      message: "LifeOS has been upgraded to a new version that includes great new things such as friends and family and you will not be alone anymore.",
    }}
  >
  {(props) => ({
      Component: NotifySlot,
      props,
  })}
  </Story>
</Canvas>
