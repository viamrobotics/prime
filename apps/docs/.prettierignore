# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Build output / generated
/dist/
/.astro/

# MDX is formatted by dprint + dprint-plugin-markdown (see dprint.json at the
# repo root). Prettier's MDX list-item indent logic mangles Starlight <Steps>
# blocks; dprint handles them correctly.
*.mdx
