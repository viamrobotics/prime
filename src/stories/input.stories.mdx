import { Canvas, Meta, Story } from '@storybook/addon-docs'

<Meta
  title="Elements/Input"
  parameters={{
    actions: {
      handles: ['input'],
    },
  }}
  argTypes={{
    type: {
      description: 'The input type',
      control: 'select',
      options: ['text', 'email', 'number', 'date', 'time', 'datetime-local'],
      table: { defaultValue: { summary: 'text' } },
    },
    label: {
      description: 'The input label',
      table: { defaultValue: { summary: '' } },
    },
    placeholder: {
      description: 'The input placeholder text',
      table: { defaultValue: { summary: '' } },
    },
    value: {
      description: 'The starting input value',
      table: { defaultValue: { summary: '' } },
    },
    step: {
      description: 'For number inputs, the increment step',
      control: 'number',
      table: { defaultValue: { summary: 1 } },
    },
    labelposition: {
      description: 'The label position',
      control: { type: 'select' },
      options: ['top', 'left'],
      table: { defaultValue: { summary: 'top' } },
    },
    'on:input': {
      description: 'Event fired whenever the input changes',
    },
  }}
/>

# Input

Used for all text single-line user inputs.

<Canvas>
  <Story
    name='Text'
    args={{
      type: 'text',
      label: 'Favorite soup',
      placeholder: 'Enter soup here',
      value: 'Minestrone',
    }}
  >
    {({ type, label, placeholder, value }) => `
      <v-input
        type='${type}'
        label='${label}'
        placeholder='${placeholder}'
        value='${value}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Number'
    args={{
      type: 'number',
      label: 'Volume of soup',
      placeholder: '0',
      value: '',
      step: 0.1,
    }}
  >
    {({ type, label, placeholder, value, step }) => `
      <v-input
        type='${type}'
        label='${label}'
        placeholder='${placeholder}'
        value='${value}'
        step='${step}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Number with slider'
    args={{
      type: 'number',
      label: 'Volume of soup',
      placeholder: '0',
      value: '',
      step: 0.1,
      incrementor: 'slider'
    }}
  >
    {({ type, label, placeholder, value, step, incrementor }) => `
      <v-input
        type='${type}'
        label='${label}'
        placeholder='${placeholder}'
        incrementor='${incrementor}'
        value='${value}'
        step='${step}'
      />
    `}
  </Story>
</Canvas>


<Canvas>
  <Story
    name='Integer'
    args={{
      type: 'integer',
      label: 'Bowls of soup',
      placeholder: '0',
      value: '',
      step: 1,
    }}
  >
    {({ type, label, placeholder, value, step }) => `
      <v-input
        type='${type}'
        label='${label}'
        placeholder='${placeholder}'
        value='${value}'
        step='${step}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Date'
    args={{
      type: 'date',
      label: 'Birthday',
      placeholder: 'Enter day here',
      value: '1985-10-22',
    }}
  >
    {({ type, label, placeholder, value }) => `
      <v-input
        type='${type}'
        label='${label}'
        placeholder='${placeholder}'
        value='${value}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Time'
    args={{
      type: 'time',
      label: 'Lunch Time',
      placeholder: '00:00:00',
      value: '12:52:48',
    }}
  >
    {({ type, label, placeholder, value }) => `
      <v-input
        type='${type}'
        label='${label}'
        placeholder='${placeholder}'
        value='${value}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Datetime Local'
    args={{
      type: 'datetime-local',
      label: 'Judgement Day',
      placeholder: '',
      value: '2022-08-09T11:03',
    }}
  >
    {({ type, label, placeholder, value }) => `
      <v-input
        type='${type}'
        label='${label}'
        placeholder='${placeholder}'
        value='${value}'
      />
    `}
  </Story>
</Canvas>



<Canvas>
  <Story
    name='No label'
    args={{
      type: 'number',
      placeholder: '0',
      value: '',
    }}
  >
    {({ type, label, placeholder, value }) => `
      <v-input
        type='${type}'
        placeholder='${placeholder}'
        value='${value}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Left label'
    args={{
      label: `Whose soup?`,
      placeholder: 'Enter name',
      value: '',
      labelPosition: 'left',
    }}
  >
    {({ type, label, placeholder, value, labelPosition }) => `
      <v-input
        label='${label}'
        placeholder='${placeholder}'
        value='${value}'
        labelposition='${labelPosition}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Left label with number'
    args={{
      type: 'number',
      label: `Soups?`,
      placeholder: 'Enter name',
      value: '',
      labelPosition: 'left',
    }}
  >
    {({ type, label, placeholder, value, labelPosition }) => `
      <v-input
        type='${type}'
        label='${label}'
        placeholder='${placeholder}'
        value='${value}'
        labelposition='${labelPosition}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Readonly'
    args={{
      label: `Least favorite soup`,
      readonly: 'readonly',
      value: 'New england clam chowder',
    }}
  >
    {({ label, readonly, value }) => `
      <v-input
        label='${label}'
        readonly='${readonly}'
        value='${value}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Tooltip'
    args={{
      label: `Favorite soup`,
      value: 'Cereal',
      tooltip: 'Unsupported soup type. Eat at your own risk. Or eat a recognized soup instead.',
    }}
  >
    {({ label, tooltip, value }) => `
      <v-input
        label='${label}'
        tooltip='${tooltip}'
        value='${value}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Disabled'
    args={{
      label: `Board`,
      value: '',
      tooltip: 'Create a board component first.',
      disabled: true,
    }}
  >
    {({ label, tooltip, value, disabled }) => `
      <v-input
        label='${label}'
        disabled='${disabled}'
        tooltip='${tooltip}'
        value='${value}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='Message'
    args={{
      label: `Board`,
      value: '',
      message: 'some message',
      state: 'info'
    }}
  >
    {({ label, value, message, state }) => `
      <v-input
        label='${label}'
        value='${value}'
        message='${message}'
        state='${state}'
      />
    `}
  </Story>
</Canvas>
