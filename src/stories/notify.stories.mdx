import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta
  title='Elements/Notify'
  parameters={{
    actions: {
      handles: ['close'],
    },
  }}
  argTypes={{
    variant: {
      control: { type: 'select' },
      options: ['danger', 'warning', 'success', 'info'],
      description: 'Notification type',
      table: { defaultValue: { summary: 'info' } },
    },
    title: {
      description: 'Title',
      table: { defaultValue: { summary: '' } },
    },
    message: {
      description: 'Message',
      table: { defaultValue: { summary: '' } },
    },
  }}
/>

# Notify

Notify message of type info, success, warning, or error

<Canvas>
  <Story
    name='info'
    args={{
      variant: 'info',
      title: 'This is the title',
      message: 'This is the message.',
    }}
  >
    {({ title, message, variant }) => `
<v-notify
  variant='${variant}'
  title='${title}'
  message='${message}'
/>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='success'
    args={{
      variant: 'success',
      title: 'This is the title',
      message: 'This is the message.',
    }}
  >
    {({ title, message, variant }) => `
<v-notify
  variant='${variant}'
  title='${title}'
  message='${message}'
/>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='warning'
    args={{
      variant: 'warning',
      title: 'This is the title',
      message: 'This is the message.',
    }}
  >
    {({ title, message, variant }) => `
<v-notify
  variant='${variant}'
  title='${title}'
  message='${message}'
/>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='danger'
    args={{
      variant: 'danger',
      title: 'This is the title',
      message: 'This is the message.',
    }}
  >
    {({ title, message, variant }) => `
<v-notify
  variant='${variant}'
  title='${title}'
  message='${message}'
/>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='no message info'
    args={{
      variant: 'info',
      title: 'This is the title',
    }}
  >
    {({ title, variant }) => `
<v-notify
  variant='${variant}'
  title='${title}'
/>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='no message success'
    args={{
      variant: 'success',
      title: 'This is the title',
    }}
  >
    {({ title, message, variant }) => `
<v-notify
  variant='${variant}'
  title='${title}'
/>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='no message warning'
    args={{
      variant: 'warning',
      title: 'This is the title',
    }}
  >
    {({ title, variant }) => `
<v-notify
  variant='${variant}'
  title='${title}'
/>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='no message danger'
    args={{
      variant: 'danger',
      title: 'This is the title',
    }}
  >
    {({ title, variant }) => `
<v-notify
  title='${title}'
  variant='${variant}'
/>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story name='Slot'>
    {() => `
<v-notify
  variant='info'
  title='You have an incoming life update'
  message='LifeOS has been upgraded to a new version that includes great new things such as friends and family and you will not be alone anymore.'
>
  <v-button slot='action' variant='inverse-primary' label='Click here'></v-button>
</v-notify>
    `}
  </Story>
</Canvas>

<Canvas>
  <Story name='Slot 2'>
    {() => `
      <v-notify variant="info" title="No logs yet." message="Make sure you have set up your part.">
        <a class='text-sm text-default font-medium hover:underline' href="">
          Set up
        </a>
      </v-notify>
    `}
  </Story>
</Canvas>