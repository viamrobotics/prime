import { Canvas, Meta, Story } from '@storybook/addon-docs'

<Meta
  title='Elements/Tooltip'
  argTypes={{
    variant: {
      description: 'The placement of the tooltip/tooltip direction',
      control: { type: 'select' },
      options: ['top', 'bottom', 'left', 'right'],
      defaultValue: '',
      table: { defaultValue: { summary: '' } }
    },
    label: {
      description: 'Optional text (tooltip will display upon hover)',
      control: 'text',
      defaultValue: 'Hover Here',
      table: { defaultValue: { summary: '' } }
    },
    icon: {
      description: 'Optional icon (tooltip will display upon hover)',
      control: 'text',
      defaultValue: '',
      table: { defaultValue: { summary: '' } }
    },
    text: {
      description: 'The message displayed within the tooltip',
      control: 'text',
      defaultValue: 'Test information for the tooltip',
      table: { defaultValue: { summary: '' } }
    },
  }}
/>

# Tooltip

Used to display text box when hovering over an element. 

<Canvas>
  <Story
    name='top'
    args={{ variant: 'top' }}
  >
    {({ variant, label, icon, text }) => `
      <v-tooltip
        variant='${variant}'
        label='${label}'
        icon='${icon}'
        text='${text}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='bottom'
    args={{ variant: 'bottom' }}
  >
    {({ variant, label, icon, text }) => `
      <v-tooltip
        variant='${variant}'
        label='${label}'
        icon='${icon}'
        text='${text}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name='right'
    args={{ variant: 'right' }}
  >
    {({ variant, label, icon, text }) => `
      <v-tooltip
        variant='${variant}'
        label='${label}'
        icon='${icon}'
        text='${text}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <div>
  </div>
  <Story
    name='left'
    args={{ variant: 'left' }}
  >
    {({ variant, label, icon, text }) => `
      <v-tooltip
        variant='${variant}'
        label='${label}'
        icon='${icon}'
        text='${text}'
      />
    `}
  </Story>
</Canvas>
