import { Canvas, Meta, Story } from '@storybook/addon-docs'

<Meta
  title='Elements/Switch'
  parameters={{
    docs: {
      description: {
        component: 'Used to handle binary input.',
      },
    },
  }}
  argTypes={{
    value: {
      control: { type: 'select' },
      options: ['on', 'off'],
      description: 'Whether or not the switch is on.',
      defaultValue: 'off',
      table: { defaultValue: { summary: 'off' } },
    },
    onInput: {
      action: 'onInput',
      description: 'Callback that fires when input value changes.',
    },
  }}
/>

<Canvas>
  <Story name='Switch'>
    {({ value }) => `
      <v-switch
        value='${value}'
      />
    `}
  </Story>
</Canvas>
