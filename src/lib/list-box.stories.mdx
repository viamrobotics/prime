<Meta
  title='Elements/ListBox'
  parameters={{
    {/* actions: {
      handles: ['input'],
    }, */}
  }}
  {/* argTypes={{
    tabs: {
      description: 'The list of tabs',
      table: { defaultValue: { summary: '' } }
    },
    selected: {
      description: 'The selected tab',
      table: { defaultValue: { summary: '' } },
    },
    'on:input': {
      description: 'Event fired when tab is selected',
    },
  }} */}
/>

# ListBox
<Canvas>
  <Story
    name='ListBox'
    args={{
    }}
  >
    {({ }) => `
      <v-list-box
      />
    `}
  </Story>
</Canvas>